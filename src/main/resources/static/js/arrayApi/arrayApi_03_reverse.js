// 3.배열을 거꾸로 출력하라
// reverse(): Reverses the elements in an Array.
// 배열안에 있는 요소를 거꾸로 출력해준다. 이는 출력만 거꾸로 바꿔주는게 아니라 배열 자체를 바꿔준다.
{
    const array = [1, 2, 3, 4, 5];
    const result = array.reverse();
    console.log(result);
    console.log(array);
}